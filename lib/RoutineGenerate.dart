import 'package:flutter/cupertino.dart';

class RoutineGenerate extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child : Center(
        child : Text('Routine Generator'),
      ),
    );
  }
}